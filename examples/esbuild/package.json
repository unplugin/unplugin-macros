{
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "build": "node build.ts",
    "watch": "node --watch build.ts",
    "dev": "node --watch dev.ts"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
